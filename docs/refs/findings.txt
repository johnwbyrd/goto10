Cycle Analysis of C64 BASIC RND(1) from Default Seed
=====================================================
Date: 2026-03-29

Starting seed: $80 $4F $C7 $52 $58 (ROM default)

Tail length (mu):      71,549  unique values before entering cycle
Cycle length (lambda): 46,813  values in the repeating cycle
Total distinct values: 118,362

The sequence forms a rho shape: 71,549 values are produced once
and never revisited, then the generator enters a permanent cycle of
46,813 values.

The theoretical state space is ~2^38 (~274 billion) reachable states.
The generator visits 118,362 of them. That is 0.000004% utilization.

Output bit distribution within one cycle:
  '\' (CHR$ 205): 23,245  (49.655%)
  '/' (CHR$ 206): 23,568  (50.345%)

The output bit sequence has the same period as the full state ---
no shorter sub-cycle exists.

At 40 columns per screen line, 25 lines per screen:
  Characters per screenful: 1,000
  Unique screenfuls before cycle: ~71.5
  Screenfuls per cycle: ~46.8
  Total screenfuls before repetition: ~118.4

Method: Brent's cycle detection algorithm
Verified: Direct seed comparison at positions mu and mu+lambda
Runtime: 0.1 seconds on modern hardware
